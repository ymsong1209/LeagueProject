syntax = "proto3";
package Protocol;

import "Enum.proto";
import "Struct.proto";

message C_LOGIN
{

}

message S_LOGIN
{
	bool success = 1;
	repeated Player players = 2; // 아이디 발급 전
}



message C_ENTER_GAME
{
	uint64 playerIndex = 1;
}

//이 패킷에서 모든 클라에게 캐릭터가 생성됐다고 보내주기.
message S_ENTER_GAME
{
	bool success = 1; //플레이어 생성 패킷 보내주기
}

message C_PLAYER_UPDATE
{

}

message S_PLAYER_UPDATE
{

}

message C_CHAT
{
	string msg = 1;
}

message S_CHAT
{
	uint64 playerId = 1;
	string msg = 2;
}